// Where the builder's server is, for app.js. Empty means "the server that served this
// page". A Netlify deploy overwrites this file with BUILDER_API_URL — see
// scripts/netlify-config.mjs and docs/HOSTING.md.
window.ARAXYS_API = window.ARAXYS_API || "";
